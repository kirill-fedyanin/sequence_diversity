defaults:
  - ood_base_config_t5
  - data: rubq_en
  - ood_data: rubq_en

ue_methods:
  type: mc_ensemble

ood:
  corrupt: true

model:
  device: 0

cache_dir: '~/rubq_rubq_mc_ensemble'
experiment_name: 'rubq_rubq_mc_ensemble'
